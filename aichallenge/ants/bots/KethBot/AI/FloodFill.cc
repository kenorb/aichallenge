#include "FloodFill.h"

Location& seekFill(int row, int col, char x)
{
    /*
    if (state.grid[row][col] == x) return Loc(row, col);
    if (state.grid[row][col] != ID_WALL) {

    }
    */
}

